let names = ["Yaakov", "John", "Jen", "Jason", 
"Paul", "Frank", "Larry", "Paula", "Laura", "Jim"];
function one() {
	if (names="Yaakov");{
		console.log ("Hello " +names);
	}
	
	
	if (names=["John", "Jen", "Jason"]);{
		for ( let i=0; i < names.length; i++) {
		console.log ("Good bay " +names[i]);
		}
	}

	if (names=["Paul", "Frank", "Larry", "Paula", "Laura"]);{
		for ( let i=0; i < names.length; i++) {
			console.log ("Hello " +names[i]);
		}
	}
		
	if (names=["Jim"]);{
		for ( let i=0; i < names.length; i++) {
		console.log ("Good bay " +names[i]);
		}
	}
	
}

one();
let x="Refresh your browser and check different names from the list"
console.log (x);




   